{{- if and .Values.mcpServer.enabled .Values.mcpServer.podDisruptionBudget.enabled }}
{{- /* PDB-singleton guardrail. See evaluator/pdb.yaml (b964c58d). */ -}}
{{- $pdb := .Values.mcpServer.podDisruptionBudget -}}
{{- $replicas := int .Values.mcpServer.replicaCount -}}
{{- if and (not $pdb.maxUnavailable) (le $replicas 1) }}
{{- fail "mcpServer.podDisruptionBudget.enabled with replicaCount=1 and minAvailable>=1 blocks ALL voluntary evictions (kubectl drain stalls forever). Either bump mcpServer.replicaCount > 1, set podDisruptionBudget.maxUnavailable=1, or disable the PDB." }}
{{- end }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.mcpServer.service.name }}
  labels:
    {{- include "kguardian.labels" . | nindent 4 }}
spec:
  {{- if $pdb.maxUnavailable }}
  maxUnavailable: {{ $pdb.maxUnavailable }}
  {{- else }}
  minAvailable: {{ $pdb.minAvailable | default 1 }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.mcpServer.service.name }}
{{- end }}
