{{- if and .Values.llmBridge.enabled .Values.llmBridge.podDisruptionBudget.enabled }}
{{- /* PDB-singleton guardrail. See evaluator/pdb.yaml (b964c58d). */ -}}
{{- $pdb := .Values.llmBridge.podDisruptionBudget -}}
{{- $replicas := int .Values.llmBridge.replicaCount -}}
{{- if and (not $pdb.maxUnavailable) (le $replicas 1) }}
{{- fail "llmBridge.podDisruptionBudget.enabled with replicaCount=1 and minAvailable>=1 blocks ALL voluntary evictions (kubectl drain stalls forever). Either bump llmBridge.replicaCount > 1, set podDisruptionBudget.maxUnavailable=1, or disable the PDB." }}
{{- end }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.llmBridge.service.name }}
  labels:
    {{- include "kguardian.labels" . | nindent 4 }}
spec:
  {{- if $pdb.maxUnavailable }}
  maxUnavailable: {{ $pdb.maxUnavailable }}
  {{- else }}
  minAvailable: {{ $pdb.minAvailable | default 1 }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.llmBridge.service.name }}
{{- end }}
